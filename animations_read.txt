   ANIMATIONS â€” anime.js showcase for the homepage   - Hero intro timeline (name, stats, orbit, photo)   - Rotating role words    /* ---------- Split a name into word spans for staggered reveal ---------- */    function splitWords(el) {        const words = el.textContent.trim().split(/\s+/);        words.forEach(w => {    /* ---------- Rotating role words ---------- */    /* ---------- Hero intro timeline ---------- */        // Split the name into words (gradient + stagger are gated behind .js-anim,        // so a split here can never leave the name invisible).        const words = nameEl ? splitWords(nameEl) : [];        const tl = anime.timeline({ easing: 'easeOutExpo', duration: 800 });          .add({ targets: words.length ? words : nameEl, opacity: [0, 1], translateY: [36, 0], duration: 650, delay: words.length ? anime.stagger(85) : 0, easing: 'easeOutBack' }, '-=350')              delay: anime.stagger(90)                    delay: anime.stagger(90, { start: 200 }),